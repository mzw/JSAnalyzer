never  {    /* <>((App_event == onComplete) && (App_event == onmousedown)) */
T0_init:
	if
	:: (((App_event == onComplete) && (App_event == onmousedown))) -> goto accept_all
	:: (1) -> goto T0_init
	fi;
accept_all:
	skip
}
