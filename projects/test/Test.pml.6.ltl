never  {    /* (App_state != handleHttpGetSeed) U (App_state == validateLogin) */
T0_init:
	if
	:: ((App_state == validateLogin)) -> goto accept_all
	:: ((App_state != handleHttpGetSeed)) -> goto T0_init
	fi;
accept_all:
	skip
}
