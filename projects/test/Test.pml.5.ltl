never  {    /* (App_event != onComplete) U (App_state == onGood) */
T0_init:
	if
	:: ((App_state == onGood)) -> goto accept_all
	:: ((App_event != onComplete)) -> goto T0_init
	fi;
accept_all:
	skip
}
