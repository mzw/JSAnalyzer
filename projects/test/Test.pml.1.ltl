never  {    /* (App_event != onload) U (App_event == onmousedown) */
T0_init:
	if
	:: ((App_event == onmousedown)) -> goto accept_all
	:: ((App_event != onload)) -> goto T0_init
	fi;
accept_all:
	skip
}
